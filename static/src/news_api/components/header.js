import React from 'react'
import LoadingBar from 'react-redux-loading-bar'

const Header = () => (
  <header>
    <LoadingBar style={{ backgroundColor: '#e5474b', height: '10px' }} />
  </header>
)

export default Header
